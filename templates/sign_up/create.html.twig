{% extends 'base.html.twig' %}
{% block body %}
<center>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Register') }}</button>
{{ form_end(form) }}
</center>
{% endblock %}
