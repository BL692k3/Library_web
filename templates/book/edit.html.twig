{% extends 'base.html.twig' %}
{% block body %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" name="edit_todo" value="Edit" class="btn btn-primary"/>
{{ form_end(form) }}
{% endblock %}
